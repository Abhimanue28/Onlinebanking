extends layout

block content
  title = "deposit_form"
  h1 DEPOSIT
  form(name="deposit", method="post", action='/deposit/#{reference_id}')
        div.input
            span.label DEPOSIT AMOUNT
            input(type="number",min="1", max="50000", name="deposit_amount",required)
        div.actions
            input(type="submit", value="DEPOSIT")
            br
            br